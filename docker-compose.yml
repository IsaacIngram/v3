version: '3.8'
services:
  frontend:
    build:
      context: ./v3-ui
    container_name: frontend
    ports:
      - "80:80"
    networks:
      - backend

  backend:
    build:
      context: ./v3-api
    container_name: backend
    networks:
      - backend
networks:
  backend: